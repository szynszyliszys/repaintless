{
  "name": "reprintless",
  "version": "1.0.0",
  "description": "Reprintless is a library of CSS animations, which don't cause costly reprints and recalculations in our browser. Animations are smooth and simple.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anna Migas",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-sass": "^2.2.0",
    "run-sequence": "^1.1.5"
  }
}
